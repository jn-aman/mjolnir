import { type LucideIcon } from 'lucide-react';
/**
 * One icon and one tint per kind, used by the sidebar, every row, the drawer
 * header and the palette, so a Deployment looks the same wherever it appears.
 */
export declare const KIND_ICON: Record<string, LucideIcon>;
export declare function kindIcon(kind: string): LucideIcon;
export declare function kindTint(kind: string): string;
//# sourceMappingURL=kindIcons.d.ts.map